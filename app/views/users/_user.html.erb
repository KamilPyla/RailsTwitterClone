    <li>
      <%= avatar_for(user) %>
      <%= link_to user.name, user %>
      <% if current_user.admin? && !current_user?(user)%>
      | <%= link_to t('admin.delete_account'), user, method: :delete, data: { confirm: t('admin.delete_account_confirm')} %>
      <% end %>
    </li>