<%= link_to avatar_for(current_user), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t('account.my_profile'), current_user %><span>
<span><%= pluralize(current_user.microposts.count, t('account.posts'))%></span>