<%= form_for(@reported_comment) do |f|%>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
  <%= f.text_area :content, placeholder: "dlaczego kurwa chcesz go zgłosić"%>
</div>
<%= f.submit t('forms.micropost.button'), class: "btn btn-primary" %>
<% end %>